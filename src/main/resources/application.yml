taco:
  orders:
    pageSize: 20
spring:
  application:
    name: taco-cloud
  profiles:
    default: dev
---
spring:
  config:
    activate:
      on-profile: dev
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
logging:
  level:
    tacos: WARN
